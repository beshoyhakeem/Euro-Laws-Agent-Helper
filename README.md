- install requirements

```
pip install -r requirements.txt

```

- to run the graph bulder

```
 python -m src.graph.graph_builder

 ```

- to run the FastApi Page

```
uvicorn app.main:app --reload --port 8080

```